{
  "name": "clinton-nato-expansion-documents",
  "version": "0.1.0",
  "private": true,
  "description": "Public research workbench for a 1000-page declassified primary-document package on NATO expansion during the Clinton administration.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-package-manifest.mjs",
    "refresh:source-register": "node scripts/build-seed-register.mjs",
    "build:package": "node scripts/build-package-manifest.mjs",
    "build:v2": "node scripts/build-version-2.mjs",
    "build:bernstein": "node scripts/build-bernstein-handoff.mjs",
    "download:package": "node scripts/download-package-pdfs.mjs",
    "test": "node scripts/validate-package.mjs",
    "check": "npm run build && npm run build:v2 && npm run build:bernstein && npm test"
  },
  "license": "CC0-1.0"
}
